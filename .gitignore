node_modules
.pnpm-store
# Every .env variant is secret-bearing, including the timestamped backups the
# RunPod reconnect writes. Only the credential-free template is tracked.
.env
.env.*
!.env.example
*.log
coverage
.DS_Store
**/dist
**/build
**/.vite
**/.cache
**/.turbo
**/tmp
**/.pytest_cache
runpodssh.txt
runpodssh.txt.pub

# Files uploaded through the chat/agent/studio composers. These are user data
# dropped into the workspace at runtime, not source.
.dacai/uploads/
